<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--advance Tree view-->
    <record id="hr_advance_view_tree" model="ir.ui.view">
        <field name="name">hr.advance.view.tree</field>
        <field name="model">hr.advance</field>
        <field name="arch" type="xml">
            <list string="Advance Requests">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="advance_amount"/>
                <field name="date"/>
                <field name="state"/>
            </list>
        </field>
    </record>
    <!--advance Form view-->
    <record id="hr_advance_view_form" model="ir.ui.view">
        <field name="name">hr.advance.view.form</field>
        <field name="model">hr.advance</field>
        <field name="arch" type="xml">
            <form string="Advance Request">
                <header>
                    <button name="action_compute_installment" type="object" string="Compute Installment" class="oe_highlight"  invisible="state in ('approve', 'refuse')"/>
                    <button name="action_submit" type="object" string="Submit" invisible="state != 'draft'" class="oe_highlight"/>
                    <button name="action_cancel" type="object" string="Cancel" invisible="state != 'draft'"/>
                    <button name="action_approve" type="object" string="Approve" invisible="state != 'waiting_approval_1'" class="oe_highlight" groups="hr.group_hr_manager"/>
                    <button name="action_refuse" type="object" string="Refuse" invisible="state != ('draft','waiting_approval_1')" class="oe_highlight" groups="hr.group_hr_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,waiting_approval_1,approve" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group col="4">
                        <field name="employee_id" readonly="state == 'approve'"/>
                        <field name="date"/>
                        <field name="department_id"/>
                        <field name="job_position_id"/>
                        <field name="advance_amount" readonly="state == 'approve'"/>
                        <field name="installment" readonly="state == 'approve'"/>
                        <field name="payment_date" readonly="state == 'approve'"/>
                        <field name="advance_disburse_date" readonly="state == 'approve'" invisible="state != 'waiting_approval_1'"/>
                        <field name="type"/>
                        <field name="description"/>
                        <field name="lines_editable" invisible="1"/>
                        <field name="company_id"
                               options="{'no_create': True}"
                               readonly="state != 'draft'"
                               groups="base.group_multi_company"/>
                        <field name="currency_id" options="{'no_create': True}" groups="base.group_multi_company"/>
                    </group>
                    <notebook>
                        <page string="Installments">
<!--                            <field name="advance_line_ids" readonly="lines_editable == True">-->
                            <field name="advance_line_ids">
                                <list string="Installments" editable="bottom">
                                    <field name="date" readonly="paid == True"/>
                                    <field name="amount" readonly="paid == True"/>
                                    <field name="paid_date" readonly="paid == True"/>
                                    <field name="paid" readonly="1" invisible="1"/>
                                </list>
                            </field>
                            <group class="oe_subtotal_footer oe_right" colspan="2" >
                                <field name="total_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="total_paid_amount" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                                <field name="balance_amount" class="oe_subtotal_footer_separator" widget="monetary" options="{'currency_field': 'currency_id'}"/>

                            </group>
                            <div class="oe_clear"/>
                        </page>
                    </notebook>
                </sheet>
                <!-- <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div> -->
            </form>
        </field>
    </record>
    <!--advance search view-->
    <record id="hr_advance_view_search" model="ir.ui.view">
        <field name="name">hr.advance.view.search</field>
        <field name="model">hr.advance</field>
        <field name="arch" type="xml">
           <search string="advance">
               <filter string="My Requests" name="myrequest" domain="[('employee_id.user_id.id', '=', uid)]" />
                <field name="employee_id" string="Employee"/>
                <field name="department_id" string="Department"/>
                    <filter string="Employee" name="employee_id" context="{'group_by':'employee_id'}"/>
                    <filter string="Department" name="department_id" context="{'group_by':'department_id'}"/>
                    <filter string="Status" name="status" context="{'group_by':'state'}"/>
            </search>
        </field>
    </record>
<!--  Record action for hr advance.  -->
    <record id="hr_advance_action" model="ir.actions.act_window">
        <field name="name">Request for Advance</field>
        <field name="res_model">hr.advance</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_myrequest': 1}</field>
        <field name="search_view_id" ref="hr_advance_view_search"/>
         <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to create a new Advance request.
            </p><p>
                Use this menu to create Advance requests.
            </p>
        </field>
    </record>
    <!-- Menu records for managing menus -->
<!--    <menuitem-->
<!--            id="hr_loan_menu_for_loan_and_advances"-->
<!--            name="Loans &amp; Advances"-->
<!--            parent="hr.menu_hr_root"-->
<!--            sequence="20"/>-->
    <menuitem
            id="hr_advance_menu_for_advance"
            name="Advance"
            parent = "ent_ohrms_loan.hr_loan_menu_for_loan_and_advances"
            sequence="1"/>
    <menuitem
            id="hr_advance_menu_creates_request_for_advance"
            name="Request for Advance"
            parent="hr_advance_menu_for_advance"
            action="hr_advance_action"/>
</odoo>
